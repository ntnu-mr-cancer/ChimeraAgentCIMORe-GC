# Deterministic + DeepEval evaluator. No torch / no CUDA — the judge LLM
# runs in the separate Ollama container with GPU access.
deepeval>=2.0.0
ollama>=0.3.0
requests>=2.31.0
graphviz>=0.20.1
scikit-learn>=1.3.0
numpy>=1.24.0
